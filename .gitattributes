# Given all files that are detected as text, convert everything to crlf
* text eol=auto

# Some random binary stuff was getting converted to crlf for some reason
*.jpg -text
*.jpeg -text
*.png -text
*.meta -text
